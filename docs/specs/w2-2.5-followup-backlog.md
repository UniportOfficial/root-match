# W2-2.5 Follow-up Backlog

> **목적**: W2-2 (Better Auth integration) atomic commit에서 분리된 follow-up 작업의 단일 진실원. W2-2가 critical-path engineer-day 5-7로 비대화되지 않도록 결합도가 낮은 항목들을 별도 wave로 분리.

| 항목             | 값                                                                   |
| ---------------- | -------------------------------------------------------------------- |
| 작성일           | 2026-06-03                                                           |
| Trigger          | W2-2 atomic commit (Better Auth 1.6 integration) 완료 + 검증 통과 후 |
| Estimated effort | 0.5–1 engineer-day (5개 Tier 1/2 항목 합산)                          |
| Wave 위치        | Wave 3a 진입 전 (W2-2 → W2-2.5 → Wave 3a 병렬)                       |
| 참고 plan        | `.sisyphus/plans/phase-1-w2.md` v0.8 §A.2 [MUST NOT DO] Gap C 항목   |
| 참고 handoff     | `docs/handoffs/2026-06-03-w2-1-complete-w2-2-ready.md` v1.1 Deferred |

---

## 1. 분리 사유 (Why W2-2.5 exists)

W2-2 atomic commit scope는 **Better Auth 1.6 integration + cookie sync + remove mock auth**로 정의됨 (`feat(api,web): better auth 1.6 integration + cookie sync + remove mock auth`). 다음 작업들은 W2-2의 직접 코드 변경에 의존하나, atomic commit 단위로 묶기에는:

- **결합도가 낮다** (별도 commit으로 분리해도 W2-2 검증 게이트에 영향 없음)
- **scope가 다르다** (코드 vs 문서, 자동화 vs 수동 절차)
- **타이밍이 늦다** (W2-2의 실제 commit 결과를 봐야 의사결정 가능)

따라서 W2-2.5 별도 wave로 묶어 처리한다. W2-2.5 commit은 **여러 개의 작은 atomic commit**으로 구성되며, 전체 wave는 1-day 이내에 완료한다.

---

## 2. 항목 분류 체계

| Tier                | 정의                                                                         | 처리 시점                   |
| ------------------- | ---------------------------------------------------------------------------- | --------------------------- |
| **Tier 1 (MUST)**   | W2-2 commit body에서 명시한 후속 작업; Wave 3a 진입 전 반드시 처리           | W2-2 직후 (Wave 3a 시작 전) |
| **Tier 2 (SHOULD)** | W2-2 결과에 따라 결정되는 follow-up; 늦어도 W2-5 commit 전까지 처리          | W2-2 결과 검증 후           |
| **Tier 3 (NICE)**   | 자동화/개선/문서 강화 항목; Phase 1.W2 마지막(W2-7) 전까지 기회 있을 때 처리 | 기회 있을 때 (max W2-7 전)  |

---

## 3. 항목 상세

### 3.1 Tier 1 — MUST (W2-2 직후)

#### 3.1.1 `apps/api/MIGRATION.md` — mock→real user 데이터 마이그레이션 playbook

**출처**: plan §7.2.3 sub-step 13 (Gap C, deferred to W2-2.5 per plan v0.8 §A.2 [MUST NOT DO]).

**Context**: Phase 1.W1에서 `apps/web/src/data/users.ts`에 2개 mock 계정이 하드코딩되어 있었음:

- `hong@techsolution.co.kr` (client, 비밀번호 없음)
- factory account (위치 확인 필요)

W2-2 완료 시점에 Better Auth signup이 live이고 `User`/`Session`/`Account`/`Verification` 테이블이 존재. 이 시점에 mock 계정들을 어떻게 처리할지 명문화 필요.

**작업 내용**:

1. `apps/api/MIGRATION.md` 작성 (NOT `apps/web/`):
   - **Section 1: Mock 계정 인벤토리** — Phase 0/W1에서 사용된 mock identities 목록 (이메일 + 역할 + 데이터 소속)
   - **Section 2: dev/CI 마이그레이션 절차** — `prisma db seed` 시 Better Auth `signUpEmail` API로 mock identities를 real users로 생성하는 절차 (W2-4에서 적용)
   - **Section 3: 비밀번호 정책** — Q7 RESOLVED 명시 (`TempPass!2026` for dev/CI, prod는 별도 절차)
   - **Section 4: prod 마이그레이션 절차 (placeholder)** — 베타 진입 전 실데이터가 없으므로 "Not Applicable for Phase 1.W2; revisit at Phase 6"로 표시
   - **Section 5: 관계 데이터 처리** — User → Company → Profile 관계 어떻게 seed 순서 결정하는지 (W2-4 sub-step 3 reference)

2. plan §7.2.3 sub-step 13 status flip: "DEFERRED" → "RESOLVED in W2-2.5 commit `<HASH>`"

**Acceptance criteria**:

- [ ] `apps/api/MIGRATION.md` 파일 존재; 5개 section 모두 채워짐
- [ ] dev/CI section은 `prisma db seed` 실행 절차와 동일 (W2-4와 정합)
- [ ] 비밀번호 정책 section은 plan §11 Q7 RESOLVED와 정합
- [ ] handoff doc Deferred 섹션에서 "§A.2 Gap C MIGRATION.md" 항목 ✅ 마킹
- [ ] plan §7.2.3 sub-step 13 status updated

**Commit**: `docs(api): w2-2.5 MIGRATION.md (mock→real user data migration playbook)`

**Estimated effort**: 1-2 hours

---

#### 3.1.2 PrismaService 패턴 결정 backfill 문서

**출처**: plan §11.1 Implementation Note 2 (Combined template at line 1142). W2-2 sub-step 1 prerequisite에서 패턴 (a) "accept dual PrismaClient" vs (b) "refactor PrismaService to composition" 중 하나를 선택. **W2-2 commit body에 어느 패턴을 선택했는지 명시되어 있어야 함**.

**Context**: Pre-Flight `aa99d30`의 `PrismaService extends PrismaClient`와 W2-2의 새 `prisma.client.ts` singleton 사이의 coexistence:

- 패턴 (a): 2 PrismaClient instances 공존 (각자 connection pool; Neon pgbouncer-level pool이 ceiling이라 MVP 영향 bounded)
- 패턴 (b): `PrismaService`를 composition으로 refactor (`readonly client = prisma`; 하나의 PrismaClient instance 공유)

**작업 내용**:

W2-2 결과에 따라 분기:

**Case (a) 선택 시** (accept dual):

1. `apps/api/src/prisma/prisma.service.ts` 헤더 주석에 다음 추가:
   ```ts
   /**
    * NOTE (W2-2 W2-2.5 follow-up): This service extends PrismaClient and coexists
    * with `prisma.client.ts` singleton used by Better Auth. Two PrismaClient
    * instances are created at runtime (one per class), each with its own internal
    * connection pool. Both pools route through Neon's pgbouncer pool, which is
    * the actual connection ceiling (MVP-acceptable). Refactor to composition
    * (`readonly client = prisma`) is deferred to Phase 2 if Connection pool churn
    * becomes a measurable issue.
    *
    * See plan §11.1 Implementation Note 2 for full context.
    */
   ```
2. `docs/specs/prisma-service-pattern.md` 신규 작성:
   - Section 1: 현재 패턴 (a) accept-dual의 trade-off
   - Section 2: pattern (b) refactor 절차 (composition 변환)
   - Section 3: trigger condition for refactor (Phase 2 traffic 측정 결과 기반)

**Case (b) 선택 시** (refactor to composition):

1. W2-2 commit 자체에 refactor가 포함되어 있을 것 (그 commit body가 설명)
2. W2-2.5에서는 `docs/specs/prisma-service-pattern.md`만 신규 작성:
   - Section 1: 패턴 (b) composition 채택 이유 (의식적 결정)
   - Section 2: PrismaService DI 가이드 (`@Global` module이 `prisma`를 어떻게 expose 하는지)
   - Section 3: 다른 모듈에서 PrismaClient 직접 import 금지 사유

**Acceptance criteria**:

- [ ] W2-2 commit body의 패턴 선택을 직접 인용하여 doc에 반영
- [ ] PrismaService 파일 헤더 (case a) 또는 doc만 (case b)
- [ ] plan §11.1 Implementation Note 2 cross-reference 명시
- [ ] 향후 refactor trigger condition 명시 (case a) 또는 일관성 유지 가이드 (case b)

**Commit**: `docs(api): w2-2.5 prisma-service pattern decision (W2-2 case (X) backfill)`

**Estimated effort**: 1-2 hours

---

#### 3.1.3 Q9 dry-run 결과 plan §11 status update

**출처**: plan §11 Q9 row currently says **(a) for now** with "first action of W2-2 is a dry-run smoke". W2-2 commit body에 dry-run 결과가 cited됨.

**작업 내용**:

W2-2 commit body의 Q9 outcome을 plan §11 Q9 row에 fold-in:

- 만약 CLI 1.4.21이 hand-authored snippet과 호환 → status: ✅ **RESOLVED-COMPATIBLE** (W2-2 사용 pipeline 명시)
- 만약 비호환 → status: ✅ **RESOLVED-FALLBACK** (hand-authored snippet 채택 명시)
- 어느 경우든 W2-2 commit hash cite

**Acceptance criteria**:

- [ ] plan §11 Q9 row update (gitignored file이지만 living doc)
- [ ] handoff doc에 Q9 결과 반영 (Deferred 또는 RESOLVED section)
- [ ] mvp-roadmap.md §1.4에 영향 (현재 "Better Auth 0%" 표현 갱신)

**Commit**: 단독 commit 불필요 (plan은 gitignored). handoff/mvp-roadmap 갱신은 다음 항목의 commit과 fold 가능.

**Estimated effort**: 30 min

---

### 3.2 Tier 2 — SHOULD (W2-2 검증 후 → W2-5 commit 전)

#### 3.2.1 Mock auth removal regression guard

**출처**: W2-2가 `apps/web/src/lib/auth-cookie.ts` 삭제 + `rm-auth`/`rm-role` cookie name 제거. 향후 regression 방지.

**작업 내용**:

1. `apps/api/scripts/check-no-mock-auth.sh` 신규 작성:

   ```bash
   #!/usr/bin/env bash
   # Fails CI if any of mock-auth artifacts re-appear
   set -euo pipefail

   FORBIDDEN=("rm-auth" "rm-role" "apps/web/src/lib/auth-cookie")
   for pattern in "${FORBIDDEN[@]}"; do
     if grep -r --include='*.{ts,tsx,js,jsx}' "$pattern" apps/ packages/ 2>/dev/null; then
       echo "ERROR: forbidden mock-auth pattern '$pattern' found"
       exit 1
     fi
   done
   echo "OK: no mock-auth artifacts"
   ```

2. `package.json` root에 script 추가:

   ```json
   "guard:no-mock-auth": "bash apps/api/scripts/check-no-mock-auth.sh"
   ```

3. `.github/workflows/ci.yml`에 새 step:
   ```yaml
   - name: Guard against mock-auth regression
     run: pnpm guard:no-mock-auth
   ```

**Acceptance criteria**:

- [ ] `pnpm guard:no-mock-auth` 통과 (현재 코드베이스 clean)
- [ ] CI에 새 step 추가
- [ ] 의도적으로 `rm-auth` 한 줄 추가 후 script 실패 확인 → revert

**Commit**: `ci: guard against mock-auth regression (W2-2.5)`

**Estimated effort**: 30 min

---

#### 3.2.2 Plan v0.9 + handoff v1.2 wave 3a 진입 prep

**작업 내용**:

W2-2 commit + W2-2.5 commits 후, plan v0.9 + handoff v1.2 작성:

1. plan v0.9 changelog row:
   - §11 Q5/Q6/Q9 status update (RESOLVED → APPLIED with W2-2 commit hash)
   - §7.2.4 verification gate: 실제 signup curl output 추가
   - §A.3 (W2-3) + §A.4 (W2-4) delegation prompts 작성 (Wave 3a parallel)
   - §16 changelog v0.9 row

2. handoff v1.2:
   - W2-2 완료 + W2-2.5 commits 요약
   - Wave 3a 진입 가이드 (2 parallel agents)
   - 새 ORCHESTRATOR ADDENDUM template (W2-3 + W2-4 각각)
   - 외부 의존성 status 업데이트

**Acceptance criteria**:

- [ ] plan v0.9 (gitignored) 작성
- [ ] handoff v1.2 (git-tracked) 작성, single commit
- [ ] 다음 세션 zero-context 진입 가능

**Commit**: `docs(handoffs): w2-2 complete, w2-3 w2-4 wave 3a ready (entry-point doc)`

**Estimated effort**: 1-2 hours

---

### 3.3 Tier 3 — NICE (기회 있을 때 → max W2-7 전)

#### 3.3.1 Husky pre-commit Node 22 PATH 자동화

**출처**: 현재 husky pre-commit hook (`pnpm lint-staged`)이 fresh subshell에서 nvm 환경을 inherit 못함. 매 commit 시 `export PATH="$HOME/.nvm/versions/node/v22.22.3/bin:$PATH"` 수동 prefix 필요.

**작업 내용**:

1. 옵션 A — `.husky/pre-commit` 수정:

   ```bash
   #!/usr/bin/env bash
   # Ensure Node 22 is on PATH for lint-staged (pnpm 11 requires Node 22+)
   if [ -f "$HOME/.nvm/nvm.sh" ]; then
     export NVM_DIR="$HOME/.nvm"
     . "$NVM_DIR/nvm.sh" --no-use
     nvm use 22 --silent || true
   fi
   exec pnpm lint-staged
   ```

2. 옵션 B — `.nvmrc` 파일 추가 (`v22.22.3`) + corepack 활용:

   ```bash
   # .nvmrc
   v22.22.3
   ```

   그리고 husky가 `nvm use` 호출하도록

3. 옵션 C — engines-strict 활용 (`.npmrc`에 `engine-strict=true`)

**비교 + 결정**:

- 옵션 A가 가장 즉시 효과 있음 (nvm 의존성 명시)
- 옵션 B는 .nvmrc만으로 부족; CI도 도움 안 됨
- 옵션 C는 install 시점 강제, commit 시점은 별도

**권장**: 옵션 A 적용; 효과 확인 후 옵션 B (`.nvmrc`)도 함께 추가.

**Acceptance criteria**:

- [ ] fresh terminal에서 `git commit` 시 prefix 없이 통과
- [ ] CI에서도 동일 hook 동작 (`pnpm install --frozen-lockfile` 후 lint-staged 통과 확인)
- [ ] README 또는 CONTRIBUTING에 한 줄 명시 (자동화됨)

**Commit**: `chore(husky): auto-load node 22 in pre-commit hook (W2-2.5)`

**Estimated effort**: 1 hour (실험 + 검증 포함)

---

#### 3.3.2 Pre-edit TDD AC list practice for Wave 3+ delegations

**출처**: Oracle round 1 (W2-1 진입 전 plan v0.7→v0.7-delta) 9 drift fixes는 pre-edit TDD AC list가 있었다면 예방 가능. W2-2 ADDENDUM에서 8-item report format으로 부분 mitigate. Wave 3+ delegations (W2-3 nestjs-zod, W2-4 seed, W2-5 modules, W2-6 security, W2-7 e2e) 전부 동일 패턴 권장.

**작업 내용**:

1. `docs/specs/delegation-ac-pattern.md` 신규 작성:
   - Section 1: Pre-edit TDD AC list 개념 (delegate가 commit 전 충족해야 할 ACs 명시)
   - Section 2: W2-1 / W2-2 pattern (8-item concise report) 분석
   - Section 3: Wave 3+ template (W2-3 ~ W2-7 각 task별 AC list seed)
   - Section 4: Anti-patterns (ACs를 너무 abstract하게 적으면 drift 못 잡음)

2. Plan §A.3, §A.4, §A.5, §A.6 (W2-3 ~ W2-6) delegation prompts에 fold-in 시 사용

**Acceptance criteria**:

- [ ] `docs/specs/delegation-ac-pattern.md` 작성
- [ ] W2-3 delegation prompt drafting 시 pattern 적용
- [ ] handoff doc Documented gaps item 1 (pre-edit TDD AC list 권장) status flip ✅

**Commit**: `docs(specs): delegation AC pattern for Wave 3+ (W2-2.5 W2-7 prep)`

**Estimated effort**: 1-2 hours

---

#### 3.3.3 2 Prisma client 버전 공존 영향 측정 + 결정

**출처**: handoff doc "무해한 warning" §3: `@prisma/client@5.22.0` (transitive via `@better-auth/cli@1.4.21`) + `@prisma/client@6.19.3` (direct, runtime). W2-2 Q9 dry-run에서 영향 확인 후 결정.

**작업 내용**:

W2-2 commit 결과에 따라:

- 만약 CLI 1.4.21 사용이 결정 (case (a) compatible) → 2 versions 공존 유지 필요. 잠재적 충돌 영역 doc화.
- 만약 hand-authored 채택 (case (b) fallback) → `@better-auth/cli` 제거 검토. 어차피 schema는 hand-authored이므로 CLI 불필요.

옵션 (b) 채택 시 commit:

```
chore(deps): remove @better-auth/cli (case (b) fallback, hand-authored schema)
```

**Acceptance criteria**:

- [ ] pnpm 의존성 트리에서 2 prisma/client 공존 상태 명시
- [ ] (b) 채택 시 CLI 제거 후 typecheck/build/test/e2e green 유지

**Estimated effort**: 30 min (case a) / 1 hour (case b)

---

### 3.4 Wave 3a + Q10 + Session C 후속 (v0.4, 2026-06-03 추가)

Wave 3a (W2-3 + W2-4) atomic commits + Session C design system + Q10 CI quality matrix 5/5 green 도달 과정에서 발견된 후속 항목. Tier 분류는 각 항목 내에 명시.

---

#### 3.4.1 [Tier 2 — SHOULD] `apps/api` test:e2e jest worker race fix

**출처**: Session B Wave 3a 통합 validation (HEAD `7360d4f`). W2-4의 `test/seed.e2e-spec.ts`가 본문에서 `runPrisma(['migrate', 'reset', '--force', '--skip-seed'])`를 호출 (line 41+). 기본 jest 병렬 worker 모드에서 `auth.e2e-spec`와 동시 실행 시 빈/생성 중 DB hit → `GET /auth/me` 500 + `afterAll`의 `prisma.user.deleteMany()` "table public.user does not exist". `--runInBand` (serial) 모드에서는 5 suites / 8 tests 모두 PASS 확인.

**Context**:

- W2-4 agent는 W2-3 landing 전에 e2e를 돌려서 다른 timing으로 우연히 race miss
- W2-3 agent는 `validation.e2e-spec` 단독으로만 돌려서 race 회피
- 본 세션 W2-3 + W2-4 결합 후 validation에서 처음 노출
- 격리 실행: `auth.e2e-spec` 단독 3/3 PASS, `validation.e2e-spec` 단독 1/1 PASS (zod v4 + better-call@1.3.5 runtime 무결)

**작업 내용** (택1):

1. **옵션 A (최소)** — `apps/api/package.json#scripts.test:e2e`에 `--runInBand` 추가:
   ```json
   "test:e2e": "jest --config ./test/jest-e2e.json --runInBand"
   ```
2. **옵션 B (jest config)** — `apps/api/test/jest-e2e.json`에 `"maxWorkers": 1` 추가
3. **옵션 C (정교)** — Worker별 DB 분리 (testcontainers 또는 schema-per-worker; W2-7 Neon CI hybrid Q3 적용 시 자연 해결 가능)

**권장**: 옵션 A (단순 + W2-7까지의 임시방편 + W2-7에서 옵션 C로 교체)

**Acceptance criteria**:

- [ ] `pnpm --filter @rootmatching/api test:e2e` 5 suites / 8 tests PASS (no flake; 5회 연속 통과 확인)
- [ ] CI quality matrix에 `test:e2e` 추가 시 (§3.4.2 참고) green 유지
- [ ] commit body에 W2-7 Q3 hybrid CI 적용 시 옵션 C 교체 follow-up 명시

**Commit**: `fix(api): test:e2e --runInBand (avoid seed.e2e migrate-reset race)`

**Estimated effort**: 15 min

**Status (v0.5, 2026-06-03)**: ✅ **CLOSED** by commit `6451145` (W2-5 atomic commit, `feat(api): users + companies modules + DTOs + e2e (W2-5)`). Fold-in applied — `apps/api/package.json#scripts.test:e2e --runInBand`. **Bonus deviation**: W2-5 agent이 `apps/api/test/seed.e2e-spec.ts`에 retry hardening (3-attempt + 1s sleep, Neon P1001 transient connection 대응) +44 lines 추가 — twin-layer closure (process serialization + transient retry). e2e 검증: 7 suites / 14 tests / 97.479s `--runInBand` 통과. W2-7 Q3 hybrid CI 적용 시 옵션 C로 교체 검토는 §3.5.5 일반화 follow-up으로 이전.

---

#### 3.4.2 [Tier 2 — SHOULD] CI quality matrix에 `test` + `test:e2e` 추가

**출처**: Session B Q10 closure observation. 현재 `.github/workflows/ci.yml` matrix는 정적 분석 + 빌드 5 jobs (lint / format:check / typecheck / build / guard:no-mock-auth)만. 단위/통합 테스트는 CI gate 아님. PR review 시점에 회귀 발견이 늦어짐.

**Context**:

- W2-4 schema migration (`add_quote_request_natural_key`) + W2-3 zod v4 같은 변경이 PR 시점에 자동 회귀 검증 안 됨
- Neon test DB 또는 ephemeral postgres (Q3 hybrid CI)가 W2-7에 적용될 때 자연스럽게 가능
- 그 전까지는 docker-compose postgres + pgvector 또는 sqlite shim 등 임시 방안 필요

**작업 내용**:

W2-7 후속으로 분리하거나, W2-5 시점에 임시 도입:

1. Ephemeral postgres (docker-compose) + pgvector → CI Job 2개 추가 (test, test:e2e)
2. `migrate reset --skip-seed` + seed → e2e suite 실행
3. matrix exclude로 lint/format 제외 (build-step만 공유)

**Acceptance criteria**:

- [ ] CI matrix에 `test` + `test:e2e` 추가 (조건부 또는 unconditional)
- [ ] PR 시점에 자동 e2e 검증
- [ ] §3.4.1 `--runInBand` 적용 후 안정성 확보

**Commit**: `ci: add test + test:e2e jobs to quality matrix (W2-7 Q3 hybrid prep)`

**Estimated effort**: 2-3 hours (docker-compose + workflow + seed env setup)

**의존**: §3.4.1 먼저, W2-7 Q3 hybrid 결정 후

---

#### 3.4.3 [Tier 2 — SHOULD] GitHub Actions Node 20 deprecation 대응 (deadline 2026-09-16)

**출처**: Session B Q10 CI run 26884509375 logs:

> Node.js 20 actions are deprecated. ... will be forced to run with Node.js 24 by default starting June 16th, 2026. Node.js 20 will be removed from the runner on September 16th, 2026.

영향 받는 actions: `actions/checkout@v4`, `actions/setup-node@v4`, `pnpm/action-setup@v4` — 현재 모두 Node 20 런타임에서 동작. 2026-09-16 deadline 이후 CI 전체 BLOCKING.

**Context**:

- Node 24 강제 시작: 2026-06-16
- Node 20 완전 제거: 2026-09-16
- 임시 회피: `FORCE_JAVASCRIPT_ACTIONS_TO_NODE24=true` env var
- 영구 해결: actions 버전 업그레이드 (Node 24 지원하는 release 필요)

**작업 내용**:

1. 각 action의 Node 24 지원 release 확인 (`gh release list -R actions/checkout` 등)
2. 호환되는 최신 버전으로 `.github/workflows/ci.yml` 업데이트
3. 만약 호환 버전 미출시 → `FORCE_JAVASCRIPT_ACTIONS_TO_NODE24=true` 임시 적용
4. CI green 재확인

**Acceptance criteria**:

- [ ] ci.yml의 3 actions 모두 Node 24 호환 버전으로 업그레이드
- [ ] CI run에 Node 20 deprecation warning 사라짐
- [ ] 2026-09-16 deadline 전 처리 완료

**Commit**: `ci: bump GH Actions to Node 24 compatible versions (deprecation deadline 2026-09-16)`

**Estimated effort**: 30 min (단순 버전 bump) ~ 1 hour (호환성 검증 포함)

---

#### 3.4.4 [Tier 2 — SHOULD] `apps/api/MIGRATION.md` ADR section — zod v4 + `better-call@1.3.5` pin

**출처**: Session B W2-3 commit `b5558a3` body:

> R1 mitigation: shared declares zod as a peerDependency and API resolves the same zod v4 instance; better-call is pinned to satisfy Better Auth runtime peers after the zod v4 move required by .meta({ id }).

W2-3 작업 중 `.meta({id})` syntax (Swagger component naming, W2-6 의존)는 zod v4 전용이라 packages/shared + apps/api 모두 v3 → v4 마이그레이션. Better Auth 1.6.13의 peer dep `better-call@1.3.5`로 명시 pin해서 runtime 호환 유지.

**작업 내용**:

`apps/api/MIGRATION.md` (현재 W2-2.5 b059cad 7 sections)에 신규 section 추가:

- **Section 8: Zod v3 → v4 마이그레이션** (W2-3에서 강제 발생)
  - 원인: `.meta({id})` syntax 요구사항 (W2-3 §7.3.3 sub-step 3)
  - 영향 패키지: `packages/shared/package.json` (peerDep zod v4), `apps/api/package.json` (deps zod v4)
  - Better Auth 호환성: `better-call@1.3.5` 명시 pin (apps/api/package.json#dependencies)
  - 검증: isolated e2e `auth.e2e-spec` 3/3 PASS + `validation.e2e-spec` 1/1 PASS + 전체 e2e `--runInBand` 5/8 PASS
  - 향후 Better Auth 1.7+ 출시 시 better-call peer 자동 추적 가능 여부 확인 권장
  - 향후 zod v5 출시 시 동일 패턴으로 마이그레이션 가능

**Acceptance criteria**:

- [ ] MIGRATION.md §8 추가 (~30-50 lines)
- [ ] W2-3 commit `b5558a3` cite + Q&A 형식 (왜 v4? 왜 better-call pin?)
- [ ] zod v5 출시 시 forward-migration 절차 placeholder

**Commit**: `docs(api): MIGRATION.md §8 (zod v4 + better-call pin ADR, W2-3 b5558a3)`

**Estimated effort**: 30-45 min

**Status (v0.5, 2026-06-03)**: ✅ **CLOSED** by commit `e47045c` (`docs(api): MIGRATION.md §8 zod v4 + better-call ADR (closes backlog §3.4.4)`, +105 lines). MIGRATION.md §8 신설 sub-sections: §8.1 Status ACCEPTED (2026-06-03) + §8.2 Context (nestjs-zod 5.4.0이 zod v4 요구 + `.meta({id})` v4 전용) + §8.3 Decision (packages/shared peerDep ^4.0.0 + apps/api devDep ^4.4.3 + better-call@1.3.5 pin) + §8.4 Alternatives 비교 (nestjs-zod 3.x downgrade / zod v3 유지 + class-validator / zod v4 + better-call pin) + §8.5 Consequences (W2-6 Swagger ID 자동 생성 unlock + Better Auth peer boundary 명시) + §8.6 Validation (Wave 3a CI 5/5 green at run 26886810257) + §8.7 References + forward-migration placeholder (zod v5).

---

#### 3.4.5 [Tier 3 — NICE] 22개 미검증 라우트 visual regression smoke

**출처**: Session C closure summary (2026-06-03):

> - Lighthouse a11y (×5 routes) ✅ 100/100/100/100/100
> - **22개 미검증 라우트 visual regression** (Phase 2 페이지 작업 중 자연 갱신)

Session C는 5 routes만 lighthouse + visual 검증. 나머지 22개 routes는 design system token 적용 후 자동 반영되지만 회귀 가능성 잔존.

**작업 내용**:

Phase 2 페이지 작업 중 자연 갱신 (별도 commit 불필요)하거나, 명시적 backlog로 처리:

1. **자연 갱신 옵션 (권장)**: Phase 2 페이지별 작업 시 Playwright smoke + lighthouse a11y check 추가. 누적 22 routes 자동 커버.
2. **명시적 backlog 옵션**: 22 routes Playwright visual snapshot baseline 생성 + CI 통합.

**Acceptance criteria**:

- [ ] Phase 2 entry-point doc (별도 doc, mvp-roadmap §3 시점)에 22 routes inventory + 작업 시 visual check 가이드 추가
- [ ] 또는 Playwright visual regression test 22개 작성 (W2-7 Q3 hybrid CI 적용 후)

**Commit**: 별도 commit 불필요 (Phase 2 자연 갱신 시), 또는 `test(web): playwright visual regression baseline (22 routes)`

**Estimated effort**: 4-6 hours (명시적 옵션) / 0 (자연 갱신)

---

#### 3.4.6 [Tier 3 — NICE] PR squash 시점에 dead commit 정리

**출처**: Session B Q10 closure 과정에서 2개 dead/superseded commit 생성:

- `b89f7a6 chore: allow approved postinstall scripts at root package.json (pnpm 11)` — pnpm 11이 `package.json#pnpm` 무시 ([pnpm/issues/9082](https://github.com/pnpm/pnpm/issues/9082)) → 효과 없음
- `bf7c0a7 fix(ci): move pnpm onlyBuiltDependencies to workspace yaml (pnpm 11)` — pnpm 11이 legacy `onlyBuiltDependencies:` 무시 → 효과 없음 (`0fe48aa`가 실제 fix)

이미 push 됐고 force push 금지 정책으로 history에 남아있음. PR squash 시점에 단일 `fix(ci): pnpm 11 build script policy + prisma generate (Q10)` 형태로 정리 가능.

**작업 내용**:

dev-monorepo → main PR 시점 (Phase 1.W2 완료 시):

1. interactive rebase 또는 PR squash merge로 5 commits (`6cb22d9`, `b89f7a6`, `bf7c0a7`, `0fe48aa`, `7360d4f`)을 1-2 commits로 압축
2. squash commit message에 Q10 closure full story 보존 (3-attempt journey + final solution)

**Acceptance criteria**:

- [ ] PR squash 시 5 → 1-2 commits
- [ ] Dead 2 commits의 hash + rationale를 squash message에 cite (감사 추적성)

**Commit**: PR merge 시 squash commit message로 처리 (단독 commit 불필요)

**Estimated effort**: 5 min (PR squash 시점)

---

### 3.5 Wave 3b W2-5 후속 (v0.5, 2026-06-03 추가)

W2-5 (Users + Companies modules, commit `6451145`) atomic commit closure 시점에 식별된 6개 follow-up. Plan `.sisyphus/plans/phase-1-w2.md` v0.12 §7.5.8 follow-ups 1-6 cross-reference.

---

#### 3.5.1 [Tier 3 — NICE] `role.mapper.ts` deferred from W2-5

**출처**: W2-5 deviation R1 (plan v0.12 §7.5.5 + §7.5.8 follow-up #1).

**Context**: W2-5는 `UserProfileSchema` 신설 (name + email + accountType + role) — legacy `UserRoleSchema=enum(['client','factory','operator'])`와 별개의 schema. Frontend가 legacy enum을 폐기하지 않은 채로 W2-5 endpoint를 호출하지 않으므로 W2-5 surface에서 mapping 불필요 → deferred.

**작업 내용**: 향후 frontend가 legacy enum을 폐기하면서 W2-5 endpoint를 호출하기 시작하면 dual-axis (role + accountType) mapping이 필요. `apps/api/src/users/role.mapper.ts` 신설 + unit test + e2e regression.

**Acceptance criteria**:

- [ ] `apps/api/src/users/role.mapper.ts` 작성 (legacy `'client'`/`'factory'`/`'operator'` → `{ role, accountType }` 변환)
- [ ] Unit test (3 cases) PASS
- [ ] e2e regression with mixed legacy-enum payloads

**Commit**: `feat(api): users role.mapper for legacy frontend enum (W2-5 follow-up)`

**Estimated effort**: 1-2 hours

**의존**: Frontend가 legacy enum 폐기 결정 후 (Phase 2+).

---

#### 3.5.2 [Tier 3 — NICE] `UserProfileUpdateSchema` scope expansion

**출처**: Plan v0.12 §7.5.8 follow-up #2.

**Context**: 현재 `UserProfileUpdateSchema = UserProfileSchema.pick({ name: true })` — `name` only. 추후 profile image / phone 등 확장 시 schema 갱신 필요.

**작업 내용**: Phase 2 UI 작업 시 필요한 field 추가 (image, phone 등). schema composition 패턴 유지 (`.pick({...})` 또는 `.partial()`). 별도 PATCH commit + e2e.

**Estimated effort**: Phase 2 자연 갱신

**의존**: Phase 2 profile UI 작업 시점.

---

#### 3.5.3 [Tier 2 — SHOULD] `Company.bizNumber` Prisma schema gap

**출처**: Plan v0.12 §7.5.8 follow-up #3. W2-5 `CompanyUpdateSchema` 10 fields에 `bizNumber`가 누락 — Prisma `Company` model에 해당 column 없음.

**Context**: 사업자등록번호는 B2B 플랫폼의 거의 필수 식별자 (전자세금계산서 발급 / 모두싸인 / 토스 escrow KYC 시 모두 사용). PRD v0.4 §6 (factory/client persona) 검토 후 필요 여부 + 추가 시점 결정.

**작업 내용**:

1. PRD v0.4 §6에서 `bizNumber` 명시 여부 확인
2. (필요시) Prisma schema migration (`alter table company add column biz_number text`) + `CompanyUpdateSchema` field 추가 + e2e case
3. 사업자등록번호 vs VAT-ID 의미 차이 명시 (한국 10자리 vs EU VAT)

**Acceptance criteria**:

- [ ] PRD §6 결정 (bizNumber 필요 vs 불필요)
- [ ] (필요시) schema migration + CompanyUpdateSchema field + e2e

**Commit**: `feat(api,prisma): Company.bizNumber column + schema gap closure (W2-5 follow-up)`

**Estimated effort**: 1-2 hours

**의존**: PRD 결정.

---

#### 3.5.4 [Tier 2 — SHOULD] `AccountType` + `CompanyRole` single SoT verification

**출처**: Plan v0.12 §7.5.8 follow-up #4. W2-5에서 `AccountTypeSchema` + `CompanyRoleSchema`를 `packages/shared/src/schemas/user.ts`로 hoist. W2-2 `auth.config.ts` enum과 동일 값 유지 필요 (Q6 dual-enum SoT 강화).

**Context**: 현재 두 곳에서 enum 정의:

- `apps/api/src/auth/auth.config.ts`: Better Auth additionalFields enum
- `packages/shared/src/schemas/user.ts`: AccountTypeSchema + CompanyRoleSchema

값 drift 방지를 위한 runtime/build-time check 필요.

**작업 내용**:

1. `assertSameSet` style runtime check 추가 (W2-2.5 시점 `apps/api/src/auth/check-additional-fields.ts` 패턴 재활용 가능 시)
2. 또는 schema에서 직접 import 하도록 `auth.config.ts` refactor (single import)

**Acceptance criteria**:

- [ ] Runtime/build-time check (값 mismatch 시 fail)
- [ ] 새 enum 값 추가 시 single edit point (또는 mismatch fail-fast)

**Commit**: `chore(api,shared): assertSameSet for AccountType+CompanyRole single SoT (W2-5 follow-up Q6 strengthening)`

**Estimated effort**: 1 hour

---

#### 3.5.5 [Tier 3 — NICE] `seed.e2e` retry hardening 일반화

**출처**: Plan v0.12 §7.5.8 follow-up #5 + W2-5 bonus deviation.

**Context**: W2-5 commit `6451145`에서 `seed.e2e-spec.ts`에 3-attempt + 1s sleep retry block 추가 (Neon P1001 transient connection 대응). 다른 e2e specs (auth, matching, users, companies)에도 동일 패턴 일반화 가능. CI matrix에 e2e 추가 시 (§3.4.2) 필수.

**작업 내용**:

1. `apps/api/test/utils/retry.ts` 신설 (재사용 helper, 3-attempt + 1s sleep generalized)
2. 다른 e2e specs에도 적용 검토 (특히 Neon connection 의존하는 setup block)
3. retry policy 문서화 (언제 retry / 언제 fail-fast)

**Estimated effort**: 1-2 hours

**의존**: §3.4.2 CI matrix 추가 시점 (W2-7 Q3 hybrid CI).

---

#### 3.5.6 [Tier 2 — SHOULD] W2-6 Swagger `.meta({id})` propagation 검증

**출처**: Plan v0.12 §7.5.8 follow-up #6 + W2-6 spec `73dbc0d` §4.

**Context**: W2-3 + W2-5에서 추가한 9개 `.meta({id})` schemas가 SwaggerModule에서 `components.schemas`에 자동 매핑되는지 W2-6 commit에서 확인 필요.

**Schema inventory** (W2-6 spec §0.4):

- W2-3 `b5558a3`: `QuoteRequestDraft`, `UserRole`, `Login`, `Register`
- W2-5 `6451145`: `UserProfile`, `UserProfileUpdate`, `AccountType`, `CompanyRole`, `CompanyUpdate`

**작업 내용**: W2-6 atomic commit (Wave 4b) 진행 시 `nestjs-zod` `cleanupOpenApiDoc` dedup 동작 검증 + `/docs` UI에서 9 schemas 모두 보이는지 확인.

**Acceptance criteria**: W2-6 verification gate에 포함 — separate commit 불필요.

**Estimated effort**: W2-6 sub-step 검증에 포함

**의존**: W2-6 commit (Wave 4b).

---

### 3.6 Wave 4b W2-6 사전 (v0.5, 2026-06-03 추가)

W2-6 (보안 + Swagger + nestjs-pino) spec v0.1 `docs/specs/w2-6-security-swagger-spec.md` (commit `73dbc0d`, 873 lines) §8 open questions + retroactive gap. W2-6 atomic commit (Wave 4b) 진행 전에 사용자 결정 필요 항목.

---

#### 3.6.1 [Tier 1 — MUST] W2-6 spec Q1-Q8 사용자 결정

**출처**: W2-6 spec `73dbc0d` §8 open questions.

**의사결정 항목** (W2-6 fire 전 결정 필요):

| Q   | 주제                                                       | 기본/제안                                                              |
| --- | ---------------------------------------------------------- | ---------------------------------------------------------------------- |
| Q1  | Throttler 기본 `ttl/limit`                                 | 30 req / 60s default + `expensive` 5 req / 60s for /matching/recommend |
| Q2  | HSTS `preload: true` 활성화 시기                           | prod-only, Phase 6 진입 시                                             |
| Q3  | Swagger `/docs` production 노출 정책                       | dev/staging only (기본)                                                |
| Q4  | `req.body.email` redaction 활성화                          | B2B privacy default ON (PII 보호)                                      |
| Q5  | `/api/auth/sign-in/email` defense-in-depth strict throttle | 추가 (5 req / 60s)                                                     |
| Q6  | Better Auth `openAPI()` Scalar UI prod 노출                | dev only (기본 false)                                                  |
| Q7  | nestjs-pino correlation ID trust mode                      | `validated incoming-trust` 권장                                        |
| Q8  | `cleanupOpenApiDoc()` 누락 (W2-3 retroactive)              | W2-6 absorb (§3.6.2 참조)                                              |

**작업 내용**:

1. 사용자가 각 Q에 대한 결정 (default 채택 또는 별도 값)
2. 결정 결과를 W2-6 spec §8에 fold-in (별도 commit으로 spec v0.2)
3. plan §A.6 (W2-6 delegation prompt) 작성 시 결정 사항 반영

**Acceptance criteria**:

- [ ] Q1-Q8 8개 결정 사항 명시
- [ ] spec v0.2 작성 (§8 update + status flip ACCEPTED)
- [ ] plan §A.6 작성 가능 상태 → Wave 4b W2-6 fire ready

**Commit**: `docs(specs): w2-6 spec v0.2 Q1-Q8 user decisions fold-in`

**Estimated effort**: 30 min (결정 수렴) + 30 min (spec v0.2 작성)

**의존**: 사용자 결정 필요.

**Status (v0.6, 2026-06-03)**: ✅ **CLOSED**. spec v0.2 commit `dbc97d7` (§8.1 Decisions sub-section 신설, 7 default + Q5 changed) + W2-6 atomic commit `ea2bd66` (`feat(api): security hardening + swagger + nestjs-pino (W2-6)`) — Q1-Q8 결정 모두 W2-6 코드에 fold-in. e2e 10 suites / 24 tests pass + Q5 twin-layer 검증 (Better Auth 자체 3/10s + NestJS auth-strict 5/60s 둘 다 429 확인) + 9 schemas (UserRole, AccountType, CompanyRole, Login, Register, UserProfile, UserProfileUpdate, CompanyUpdate, QuoteRequestDraft) `/docs-json` components.schemas emit 확인.

---

#### 3.6.2 [Tier 2 — SHOULD] W2-3 `cleanupOpenApiDoc()` 누락 — W2-6에서 absorb

**출처**: W2-6 spec `73dbc0d` §0.2 + §6.1 sub-step 6.

**Context**: W2-3 commit `b5558a3` 시점에 nestjs-zod 5.4.0 + `createZodDto()` 도입했으나 `cleanupOpenApiDoc()` apply 누락. Swagger schema 생성 시 dedup 누락 → `components.schemas`에 중복 항목 가능성. W2-6 SwaggerModule.createDocument 시점에 한 번에 처리하는 게 retroactive backlog보다 효율적.

**작업 내용**: W2-6 commit (Wave 4b)에서 `SwaggerModule.setup('docs', app, cleanupOpenApiDoc(SwaggerModule.createDocument(app, config)))` 패턴 적용. 별도 backlog commit 불필요.

**Acceptance criteria**:

- [ ] W2-6 commit body에 cleanupOpenApiDoc retroactive 처리 명시
- [ ] `/docs` UI에서 dedup된 schemas (9개 + Q1-Q8 결정에 따른 추가) 확인

**Commit**: W2-6 atomic commit에 fold-in (별도 commit 없음)

**Estimated effort**: W2-6 sub-step 6에 포함 (additional effort ~0).

**Status (v0.6, 2026-06-03)**: ✅ **CLOSED** by W2-6 commit `ea2bd66`. `cleanupOpenApiDoc()` import + apply가 `apps/api/src/main.ts` (또는 bootstrap helper)에서 적용됨. W2-3 retroactive backlog 별도 commit 불필요 — W2-6에 absorb 완료. 9 schemas `/docs-json` components.schemas emit 확인 (live curl 검증).

---

### 3.7 Phase 3 전자계약 vendor (UCanSign) 통합 prep (v0.7, 2026-06-04)

Phase 3 (전자계약) 진입 전 vendor 결정 + sandbox + plan 준비. 핵심 spec: `docs/specs/ucansign-api-reference.md` v0.1.

---

#### 3.7.1 [Tier 1 — ✅ CLOSED v0.7] Phase 3 vendor 결정 — UCanSign 채택

**출처**: PRD v0.4 §6.2 + handoff §3.4 외부 dep #2.

**Context**: PRD v0.4는 "모두싸인 / 이폼사인" vendor 후보 명시. 사용자가 결정한 vendor는 **UCanSign (유캔싸인, `ucansign.com`)** — Postman 공식 문서 (publishedId `2s7YfHhcGY`, 2022-09-15) 제공.

**Status (v0.7)**: ✅ **CLOSED**. `docs/specs/ucansign-api-reference.md` v0.1 작성으로 vendor 결정 closure. 외부 dep #2 vendor selection 완료.

**적합성 검증** (ucansign-api-reference §3):

- PRD FR-5 P0 5/5 cover (전자계약 작성 / 전자서명 / PDF 다운로드 / 상태 머신 / 표준 약관 템플릿)
- Webhook 4 events 명시 지원 (signing_creating / signing_canceled / signing_completed / signing_completed_all)
- 임베딩 페이지 4 endpoints (iframe 통합)
- customValue × 6 필드 (multi-tenant 매핑 용이)
- 가격 명확: 1건당 100원 + 신규 10건 무료 포인트
- audit-trail PDF 지원 (단 §4의2 법적 효력은 vendor 확인 needed — §3.7.2 Q4)
- integrity_hash 자체 제공 X → rootmatching 자체 계산 패턴 (§3.7.7)

---

#### 3.7.2 [Tier 1 — MUST] UCanSign Tier 1 CRITICAL 4건 vendor 문의

**출처**: `docs/specs/ucansign-api-reference.md` §5.1-§5.5 + `docs/vendor-inquiries/ucansign-2026-06-04.md` §4 Q1-Q4.

**Context**: Phase 3 진입 전 blocker 4건. 가이드에 명시되지 않은 항목.

**작업 내용**:

1. vendor 채널톡/지원팀에 다음 4건 문의 (이미 mock 문의서 `docs/vendor-inquiries/ucansign-2026-06-04.md` v1.0 작성됨, 운영 시 사용자 정보 교체 후 송부):
   - **Q1 외국인 본인인증 지원 여부** — `mobile_identification`이 외국인 휴대폰 + 외국인등록번호 대응 가능?
   - **Q2 Sandbox / Dev 환경 정책** — 별도 dev API KEY / 별도 sandbox URL / 무료 한도
   - **Q3 Webhook signing secret + payload + retry** — HMAC-SHA256? 어느 헤더? 재시도 정책?
   - **Q4 audit-trail = 전자문서법 §4의2 감사추적인증서 법적 효력** — 명시적 확인
2. ucansign-api-reference v0.2에 fold-in

**Acceptance criteria**:

- [ ] vendor 답변 확보 + ucansign-api-reference §5.1-§5.5 결정 fold-in (v0.2)
- [ ] PRD FR-5.2 wording 갱신 ("UCanSign webhook" 명확)
- [ ] webhook signing secret 검증 패턴 결정 (vendor 미제공 시 IP whitelist + customValue5 매칭 fallback)

**Estimated effort**: vendor 응답 lead time + 1시간 문서화

**의존**: vendor 응답 needed.

---

#### 3.7.3 [Tier 2 — SHOULD] UCanSign Tier 2 4건 vendor 문의

**출처**: `docs/specs/ucansign-api-reference.md` §5.3 + §5.4 + §5.6 + §5.8 + `docs/vendor-inquiries/ucansign-2026-06-04.md` §5 Q5-Q8.

**Context**: Tier 1 4건 (§3.7.2) 외 Phase 3 design 영향 SHOULD 항목.

**작업 내용**:

1. vendor에 다음 4건 추가 문의 (mock inquiry 송부 시 §5에 포함):
   - **Q5 가격 / 사용량 정책 상세** — 포인트 충전 단위 + 월 한도 + 결제 주기 + 추가 비용 항목
   - **Q6 변경계약서 (FR-5.6 P1)** — cancel+재생성 vs 별도 endpoint
   - **Q7 integrity_hash (SHA-256) 자체 제공 여부** — 미제공 시 자체 계산 패턴 법적 효력
   - **Q8 Multi-tenant 권한 + locale** — 단일 운영 조직 키 정책 OK 여부
2. ucansign-api-reference v0.2에 fold-in

**Acceptance criteria**:

- [ ] sandbox key 발급 또는 dev 환경 가이드 확보 (Q2 fold-in)
- [ ] 가격 정책 확보 (사업 회계 — Q5)
- [ ] 변경계약서 정책 결정 (Q6 — P1 영역이라 P0 영향 없음)
- [ ] integrity_hash 자체 계산 패턴 확정 (Q7)
- [ ] multi-tenant 권한 결정 (Q8)

**Estimated effort**: vendor 응답 lead time + 1.5시간 문서화

**의존**: vendor 응답 needed (§3.7.2 + §3.7.3 병행 송부 가능).

---

#### 3.7.4 [Tier 2 — SHOULD] PRD v0.5 갱신 (vendor + Phase 5 scope 영향)

**출처**: ucansign-api-reference §7.

**Context**: vendor 결정 결과 PRD에 반영 + Phase 5 algorithm scope.

**작업 내용**:

1. PRD v0.4 → v0.5:
   - §6.2 P0 "전자계약 + 전자서명": "모두싸인 / 이폼사인" → "**UCanSign (유캔싸인)**" 명시
   - §FR-5.2: "모두싸인 / 이폼사인 webhook" → "UCanSign webhook (4 events: sign_creating / signing_canceled / signing_completed / signing_completed_all)"
   - §FR-11.3 (P1) 또는 §15에 sub-section: "전자계약 서명 요청 알림은 UCanSign signingMethodType=kakao 활용 (제한적); 계약 완료 알림 + 비계약 거래 알림은 rootmatching 자체 (Bizmsg/NHN)"
   - 변경 이력 row v0.5 추가
2. mvp-roadmap.md v1.3 update:
   - §2.3 Phase 3 §3.1: "vendor 선정 기준 비교 표" → "UCanSign 채택 확정 (ucansign-api-reference §3 P0 5/5 cover)"
   - §10 외부 의존성 #2 row: ⏸ 미착수 → ⏳ vendor 결정 완료 (UCanSign), Tier 1 4건 + Tier 2 4건 확인 대기

**Acceptance criteria**:

- [ ] PRD v0.5 commit (영문 subject + 한국어 본문)
- [ ] mvp-roadmap.md v1.3 commit

**Commit**: 분리 commit 권장 — `docs(prd): v0.5 — UCanSign vendor decision + Phase 5 algorithm scope` + `docs(plans): mvp-roadmap v1.3 (UCanSign vendor closure + external dep #2 status)`

**Estimated effort**: 1-2시간

**의존**: §3.7.2/§3.7.3 답변 후 (또는 wording에 "TBD by vendor"로 일단 commit)

---

#### 3.7.5 [Tier 2 — SHOULD] plan §A.7 W2-Phase3 delegation prompt 작성

**출처**: ucansign-api-reference §6.2 sub-steps 11단계.

**Context**: Phase 3 진입 시 ContractsModule delegation 위한 prompt skeleton. ucansign-api-reference §6.2를 plan으로 hoist + ORCHESTRATOR ADDENDUM template.

**작업 내용**:

1. `.sisyphus/plans/phase-1-w2.md` 또는 `phase-2-and-beyond.md` (새 plan)에 §A.7 신설:
   - [TASK] / [EXPECTED OUTCOME — 11 sub-steps] / [REQUIRED TOOLS] / [MUST DO] / [MUST NOT DO] / [CONTEXT]
   - ORCHESTRATOR ADDENDUM (HEAD + W2-6 closure + vendor decision UCanSign + open questions resolution status)
   - 8-item concise report format
2. 본 작업은 **gitignored plan 영역** — commit 없음, local edit only

**Acceptance criteria**:

- [ ] plan §A.7 작성 (ucansign-api-reference §6 hoist)
- [ ] W2-6 + Phase 2 (견적/매칭 persist) closure 후 즉시 fire 가능 상태

**Commit**: gitignored plan — 별도 commit 없음

**Estimated effort**: 1-2시간

**의존**: W2-6 closure 후 (현재 closed `ea2bd66`) + Phase 2 closure 후 (미시작)

---

#### 3.7.6 [Tier 2 — SHOULD] integrity_hash 자체 계산 패턴 spec

**출처**: ucansign-api-reference §3 부가 (integrity_hash) + §4.4 step 4 + §5.8.

**Context**: UCanSign 가이드에 integrity_hash 응답 필드 미명시. 분쟁 시 PDF 무결성 검증을 위한 자체 계산 패턴 needed.

**작업 내용**:

1. ucansign-api-reference §4.4 워크플로 step 4 (signing_completed_all event 후):
   - vendor에서 PDF (`/documents/:id/file`) + audit-trail PDF (`/documents/:id/audit-trail`) 다운로드
   - 두 파일 binary concat
   - SHA-256 계산 → `Contract.pdfSha256Hash` 컬럼에 저장
2. Phase 3 구현 시:
   - `apps/api/src/contracts/contracts.service.ts`에 `computeIntegrityHash(contractId)` 메서드
   - e2e test에서 동일 contract → 동일 hash 검증
   - 분쟁 발생 시 (Phase 5) hash 재계산 → DB 저장값과 비교
3. PRD §FR-5 또는 §FR-8 (분쟁)에 "integrity_hash 자체 계산" 명시 fold-in

**Acceptance criteria**:

- [ ] ucansign-api-reference §4.4 step 4 자체 계산 로직 명시 ✅ (이미 작성)
- [ ] Phase 3 구현 시 `computeIntegrityHash` 메서드 + e2e test
- [ ] PRD §FR-8 분쟁 시 hash 검증 패턴 명시 (PRD v0.5 fold-in)

**Commit**: Phase 3 구현 atomic commit + PRD v0.5 commit에 fold-in

**Estimated effort**: Phase 3 sub-step 9 (contracts.e2e-spec.ts)에 흡수 — additional ~30분

**의존**: Phase 3 진입 시점

---

## 4. Out of scope (W2-2.5에 포함하지 않을 것)

| 항목                                             | 어디로?             |
| ------------------------------------------------ | ------------------- |
| W2-3 (nestjs-zod DTO)                            | Wave 3a (병렬 후속) |
| W2-4 (Prisma seed)                               | Wave 3a (병렬 후속) |
| Q11 (`prisma.config.ts` migration)               | Prisma 7 upgrade 시 |
| Throttler Redis migration                        | Multi-instance 시   |
| 기타 외부 의존성 (전자서명/토스/알림톡)          | Phase 2-5           |
| 3 `<img>` LCP lint warning                       | Phase 2 UI 작업 시  |
| Neon region us-east-2 → ap-northeast-2 migration | Prod 진입 시        |

---

## 5. Execution order (W2-2 → Wave 3a 사이)

W2-2 atomic commit + 검증 통과 후, 다음 순서로 W2-2.5 실행:

```text
[W2-2 검증 통과]
   ↓
W2-2.5 Step 1: Tier 1 항목 처리 (3개)
   ├─ 3.1.1 MIGRATION.md
   ├─ 3.1.2 PrismaService pattern 결정 backfill
   └─ 3.1.3 Q9 dry-run plan §11 status update
   (atomic commits 2-3개, 합산 0.5d 이내)
   ↓
W2-2.5 Step 2: Tier 2 항목 처리 (2개)
   ├─ 3.2.1 mock auth regression guard CI
   └─ 3.2.2 plan v0.9 + handoff v1.2 (Wave 3a prep)
   (atomic commits 2개, 합산 0.5d 이내)
   ↓
W2-2.5 Step 3: (선택) Tier 3 항목
   ├─ 3.3.1 husky Node 22 자동화
   ├─ 3.3.2 delegation AC pattern doc
   └─ 3.3.3 prisma client 2 versions 결정
   (각자 별도 commit; 기회 있을 때 — 다음 wave 가는 길에)
   ↓
Push all (Stage A delayed from Option B)
   ├─ git push origin dev-monorepo
   └─ gh run watch (CI 통과 확인, Q10 fix validation)
   ↓
[Wave 3a 진입: W2-3 ∥ W2-4 fire]
```

**Tier 1 + Tier 2 합산 effort**: ~1 engineer-day  
**Tier 3 포함 시**: ~1.5 engineer-day

---

## 6. Verification (W2-2.5 wave 완료 기준)

W2-2.5 wave 완료의 binary 기준:

- [x] **Tier 1 항목 3개 모두 처리** (✅ commits `b059cad`, `23d917a`, `5c0b536`) — 2026-06-03
  - [x] §3.1.1 `apps/api/MIGRATION.md` (7 sections, 352 lines) — `b059cad`
  - [x] §3.1.2 PrismaService Pattern (a) backfill (header comment + spec doc 246 lines) — `23d917a`
  - [x] §3.1.3 Q9 status update (handoff Q9 row + mvp-roadmap §1.3/§2.1 + backlog v0.2) — `5c0b536`
- [x] **Tier 2 항목 2개 모두 처리** (✅ commits `bec8a14`, `02f8178`) — 2026-06-03
  - [x] §3.2.1 mock auth regression guard CI green — `bec8a14` (script + pnpm script + ci.yml matrix; local PASS + intentional regression FAIL verified)
  - [x] §3.2.2 plan v0.10 (gitignored: §11 Q5/Q6 APPLIED + §7.2.4 actual result + §A.3 W2-3 + §A.4 W2-4 prompts + §16 v0.10) + handoff v1.2 (new entry-point doc) — `02f8178`
- [x] **Wave 3a 진입 가능 상태** — W2-3 (§A.3) + W2-4 (§A.4) delegation prompts ready; ORCHESTRATOR ADDENDUM templates in handoff v1.2 §2.2.4 + §2.3.4
- [ ] git push 성공 + CI green (Q10 fix validation pass) — push 정책 명시 요청 시에만

Tier 3은 skip 가능; Wave 3+ 가는 길에 fold-in.

---

## 7. 참고 문서

| 문서                                                   | 관련 섹션                                                |
| ------------------------------------------------------ | -------------------------------------------------------- |
| `.sisyphus/plans/phase-1-w2.md` v0.8                   | §7.2 (W2-2 sub-steps), §A.2 (W2-2 prompt), §11.1, §11 Q9 |
| `docs/handoffs/2026-06-03-w2-1-complete-w2-2-ready.md` | v1.1 Deferred / Documented gaps                          |
| `docs/plans/mvp-roadmap.md`                            | §1.4 (현재 미구현 인벤토리)                              |
| `docs/specs/matching-endpoint-design-decision.md`      | Option C α/β reference                                   |

---

## 8. 변경 이력

| 버전 | 날짜       | 변경                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                |
| ---- | ---------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| v0.7 | 2026-06-04 | Phase 3 vendor 결정 UCanSign 채택. §3.7 정리 (UCanSign 단일 vendor) + §3.7.6 integrity_hash 자체 계산 패턴 신설. ucansign-api-reference v0.1 + vendor inquiry mock 추가. 외부 dep #2 status: 🔄 vendor 결정 완료, 8 questions 응답 대기.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                            |
| v0.6 | 2026-06-03 | **W2-6 atomic commit `ea2bd66` 통합 closure + §3.7 신설.** (a) §3.6.1 ✅ **CLOSED** — W2-6 commit `ea2bd66` (`feat(api): security hardening + swagger + nestjs-pino (W2-6)`)이 spec v0.2 §8.1 Q1-Q8 결정 모두 fold-in 완료: throttler 3 buckets + helmet + cookieParser + nestjs-pino + Swagger 3.1.0 + Better Auth Scalar UI + cleanupOpenApiDoc (W2-3 retroactive). 10 e2e suites / 24 tests pass + Q5 twin-layer 검증 + 9 schemas `/docs-json` emit 확인. (b) §3.6.2 ✅ **CLOSED** — `cleanupOpenApiDoc()` retroactive를 W2-6 commit `ea2bd66`에 absorb. (c) §3.7 신규 — Phase 3 전자계약 vendor 통합 prep 6 항목. (d) **외부 dep #2** (handoff §3.4) status: ⏸ 미착수 → ⏳ vendor 결정 진행 중.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                 |
| ---- | ---------- | ----------                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                          |
| v0.5 | 2026-06-03 | **Wave 3a/Q10/Session C + Wave 3b W2-5 + Tier 1 parallel side-quests 통합 closure. §3.4.1 + §3.4.4 ✅ CLOSED + §3.5 (W2-5 후속 6 항목) + §3.6 (W2-6 사전 2 항목) 신설.** (a) §3.4.1 ✅ CLOSED — commit `6451145` (W2-5 atomic commit, `feat(api): users + companies modules + DTOs + e2e (W2-5)`) fold-in: `apps/api/package.json#scripts.test:e2e --runInBand`. Bonus: W2-5 agent이 `apps/api/test/seed.e2e-spec.ts`에 retry hardening (3-attempt + 1s sleep, Neon P1001 transient 대응) +44 lines 추가 — twin-layer closure (process serialization + transient retry). (b) §3.4.4 ✅ CLOSED — commit `e47045c` (`docs(api): MIGRATION.md §8 zod v4 + better-call ADR`, +105 lines, §8.1-§8.7 sub-sections 신설). (c) §3.5 신규 — Wave 3b W2-5 후속 6 항목 (plan v0.12 §7.5.8 follow-ups cross-ref): role.mapper.ts deferred (§3.5.1) / UserProfileUpdate scope (§3.5.2) / Company.bizNumber gap (§3.5.3) / AccountType+CompanyRole single SoT (§3.5.4) / seed.e2e retry hardening 일반화 (§3.5.5) / W2-6 Swagger .meta({id}) propagation (§3.5.6). (d) §3.6 신규 — Wave 4b W2-6 사전 2 항목: Q1-Q8 사용자 결정 (§3.6.1, W2-6 fire 전 필요; Throttler/HSTS/Swagger prod/email redaction/signin throttle/Better Auth Scalar UI/correlation trust/cleanupOpenApiDoc retroactive 8건) + W2-3 cleanupOpenApiDoc retroactive (§3.6.2, W2-6 absorb 권장). (e) Tier 1 side-quests 통합 reference: #1 MIGRATION §8 (`e47045c`) + #2 plan v0.11→v0.12 (gitignored) + #3 W2-6 spec v0.1 (`73dbc0d`, 873 lines) + #4 Phase 2 follow-up specs (`08984ff`, +435) + #5 mvp-roadmap v1.2 (`78d0bfa`, +215/-47) + #6 quotes route grouping decision (`7cdf6c2`, +213). (f) 다음 작업 reference: plan §A.6 (W2-6 delegation prompt 정식 작성 — Q1-Q8 결정 후) + W2-6 fire (Wave 4b).                                |
| ---- | ---------- | ----------                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                          |
| v0.4 | 2026-06-03 | **Wave 3a + Q10 CI closure + Session C 통합 완료. §3.4 신규 섹션 6 항목 추가.** (a) Wave 3a (W2-3 + W2-4) origin 반영 — `b5558a3` nestjs-zod global validation + matching/auth DTOs (11 files, +179/-68; 8/8 sub-steps + curl gate HTTP 400 + 7 verification gates) + `1b37cbe` prisma seed via better-auth signup (9 files, +340/-8; 9/9 sub-steps + idempotent `2 2 3` live gate + schema migration `add_quote_request_natural_key`). (b) Session C origin 반영 — `3679a34` design system upgrade (toss tokens + senior-friendly WCAG AAA): apps/web 7 files + docs/design-system.md; Lighthouse a11y ×5 routes = 100/100/100/100/100; WCAG AAA contrast 11:1/16.56:1/8.17:1/7.59:1; Playwright signup→/dashboard smoke 3.7s. (c) Q10 CI closure (5-commit cascade) — `6cb22d9` dev-monorepo push trigger unlock + `b89f7a6` dead (pnpm 11 ignores `package.json#pnpm`; pnpm/issues/9082) + `bf7c0a7` dead (pnpm 11 ignores legacy `onlyBuiltDependencies:` list) + `0fe48aa` REAL fix (`allowBuilds:` map per pnpm 11 release notes) + `7360d4f` REAL fix (`apps/api` postinstall `prisma generate`). CI run 26884509375 ALL 5 jobs green: lint 31s, format:check 35s, typecheck 39s, build 55s, guard:no-mock-auth 25s. (d) §3.4 신규 — 6개 후속 항목 (Tier 2: 4 + Tier 3: 2): §3.4.1 `test:e2e --runInBand` (seed.e2e migrate-reset race), §3.4.2 CI matrix에 test+test:e2e 추가, §3.4.3 GH Actions Node 20 deprecation (deadline 2026-09-16), §3.4.4 MIGRATION.md §8 zod v4 + better-call@1.3.5 ADR, §3.4.5 22개 미검증 라우트 visual regression (Phase 2 자연 갱신), §3.4.6 PR squash 시 dead commit 정리. 본 backlog 자체는 W2-2.5 scope 유지 — 새 항목은 §3.4로 grouping. 다음 작업 reference: handoff `docs/handoffs/2026-06-03-wave-3a-q10-session-c-complete.md` v1.0.                  |
| ---- | ---------- | ----------                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                          |
| v0.3 | 2026-06-03 | **Tier 2 (SHOULD) 2개 항목 모두 완료 + Wave 3a 진입 준비 완료.** (a) §3.2.1 mock auth regression guard CI — commit `bec8a14`: `apps/api/scripts/check-no-mock-auth.sh` (96 lines, executable, bash man-page header + 3 forbidden patterns + scope boundary docs) + `package.json` root script + `.github/workflows/ci.yml` matrix task `guard:no-mock-auth` (build skip added). Local PASS + 의도적 regression test FAIL (exit 1 with file:line evidence + remediation hint + revert verified). (b) §3.2.2 plan v0.10 + handoff v1.2 — commit `02f8178`: 새 entry-point doc `docs/handoffs/2026-06-03-w2-2-complete-wave-3a-ready.md` (517 lines, 8 sections) — W2-2 closure + W2-2.5 Tier 1+2 summary + Wave 3a delegation guide (W2-3 + W2-4 calls + prompt bodies + ORCHESTRATOR ADDENDUM templates §2.2.4/§2.3.4) + 외부 의존성 + Deferred + 4 first-message candidates. Plan v0.10 (gitignored): §11 Q5/Q6 status RESOLVED → APPLIED with `f484ad5` reference + W2-2 deviation 문서화 (`[...]` spread for `DBFieldType` mutability + 5 regression cases pass); §7.2.4 verification gate에 "Actual W2-2 result" block 추가 (verbatim HTTP/1.1 200 + Set-Cookie + payload from commit body); §A.3 (NEW) W2-3 nestjs-zod prompt (8 EXPECTED OUTCOMES 1:1 with §7.3.3 sub-steps; category `quick` + skill `git-master`); §A.4 (NEW) W2-4 Prisma seed prompt (9 EXPECTED OUTCOMES 1:1 with §7.4.3 sub-steps + cross-ref `apps/api/MIGRATION.md` as canonical playbook; category `unspecified-high`); §16 v0.10 changelog row; footer v0.10. Plan: 1620 → 1782 lines (+162). 이전 entry-point `2026-06-03-w2-1-complete-w2-2-ready.md` v1.1은 historical reference로 보존. **Tier 3 잔여** (optional): §3.3.1 husky Node 22 자동화 + §3.3.2 delegation AC pattern doc + §3.3.3 2 prisma client 결정. |
| ---- | ---------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------                                                                                                                                                                                                                                                                                                                                           |
| v0.2 | 2026-06-03 | **Tier 1 (MUST) 3개 항목 모두 완료.** (a) §3.1.1 `apps/api/MIGRATION.md` 작성 — commit `b059cad` (7 sections, 352 lines): mock 계정 2개 인벤토리 + W2-4 dev/CI 마이그레이션 절차 (`auth.api.signUpEmail` + idempotency ensureXxx) + 비밀번호 정책 (TempPass!2026 dev/CI per Q7) + prod placeholder (Phase 6 베타 진입 시 구체화; Option A/B/C 후보) + 관계 데이터 처리 순서 (User → Profile → Company → FactoryEmbedding → QuoteRequest → MatchRecommendation) + 검증 매트릭스. (b) §3.1.2 PrismaService Pattern (a) backfill — commit `23d917a`: header comment 20 lines 추가 (`apps/api/src/prisma/prisma.service.ts`) + `docs/specs/prisma-service-pattern.md` 신규 작성 (246 lines, 6 sections): §1 현재 trade-off + 컨센션 표 + §2 Pattern (b) refactor 3-step 절차 + ast-grep codemod + §3 trigger conditions (Phase / Measurement / Code-quality + decision matrix) + §4 grep tags + §5 references + §6 변경 이력. (c) §3.1.3 Q9 dry-run status — handoff doc Q9 row "INCOMPATIBLE → fallback (c)" + Deferred 섹션 §A.2 Gap C ✅ W2-2.5 applied 마킹 + Pattern (a) backfill ✅ 마킹 + Pattern (a)→(b) refactor 추가; mvp-roadmap.md §1.3 "Phase 1.W2 진행 중 업데이트" 재작성 (Better Auth ✅ + Prisma ✅ + Neon ✅) + §2.1 W2-1/W2-2 ✅ + W2-2.5 추가 + 진행도 명시 (~8-10 engineer-day 완료). **Tier 2/3 잔여**: W2-2.5 §3.2 (mock auth regression CI + plan v0.9 + handoff v1.2) + §3.3 (husky 자동화 + AC pattern doc + 2 prisma client 결정).                                                                                                                                                                                                                                                                                                                                           |
| v0.1 | 2026-06-03 | 초기 작성. Stage B (plan v0.8) 후속으로 W2-2.5 항목 정리. 3 Tier 구조 (MUST/SHOULD/NICE) + 8개 항목 + execution order + verification 기준 정의.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                     |
