NEXT_PUBLIC_API_URL=http://localhost:3001

# Contract template id used when calling POST /contracts. Falls back to
# "mock-template" — accepted by the mock gateway but rejected by the real
# vendor. Set this to the UCanSign template id once provisioned (STEP 3).
# NEXT_PUBLIC_CONTRACT_TEMPLATE_ID=
